import { CardHeaderTitleDirective } from './card-header-title.directive';

describe('CardHeaderTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new CardHeaderTitleDirective();
    expect(directive).toBeTruthy();
  });
});
