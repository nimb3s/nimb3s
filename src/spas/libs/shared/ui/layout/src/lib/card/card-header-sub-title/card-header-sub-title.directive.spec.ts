import { CardHeaderSubTitleDirective } from './card-header-sub-title.directive';

describe('CardHeaderSubTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new CardHeaderSubTitleDirective();
    expect(directive).toBeTruthy();
  });
});
